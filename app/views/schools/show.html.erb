<<<<<<< HEAD
<div class="container">
  <div class="header">
    <div class="col-xs-12 col-sm-4">
      <%= image_tag @school.picture.url(:medium) %>
    </div>
      <div class="col-xs-12 col-sm-4">
        <h2><%= @school.name %></h2>
        <p><%= @school.address %></p>
        <p><%= @school.phone %></p>
      </div>
        <div class="col-xs-12 col-sm-4">
          <h4>liste des professeurs de l'école</h4>
          <% @school.teachers.each do |teacher| %>
            <ul>
              <li> <%=link_to teacher.name, teacher_path(teacher) %></li>
            </ul>
          <% end %>
            <%= link_to 'choisissez un nouveau moniteur', school_add_contract_path(@school) %>
        </div>
    </div>
  </div>
</div>


  <div id="map" style="width: 50%; height: 300px;"></div>
</div>

  <% content_for(:js) do %>
    <%= javascript_tag do %>
      $(document).on('ready', function() {
        handler = Gmaps.build('Google');
        handler.buildMap({ internal: { id: 'map' } }, function(){
          markers = handler.addMarkers(<%= raw @markers.to_json %>);
          handler.bounds.extendWith(markers);
          handler.fitMapToBounds();
        });
      })
    <% end %>
  <% end %>
=======
  <div class="container text-center">
    <div class="row">
      <div class="col-xs-12">
        <div class="uppic">
          <%= image_tag @school.picture.url(:medium) %>
        </div>
          <h1><%= @school.name %></h1>
          <p><%= @school.address %></p>
          <p><%= @school.phone %></p>
          <p><%= @school.manager_name %></p>
      </div>


    <br>
      <h2>liste des professeurs de l'école</h2>
    <br>

    <table>
     <thead>
       <tr>
         <th>Name</th>
         <th>E-mail</th>
         <th colspan="3"></th>
       </tr>
     </thead>  <tbody>
       <% @school.teachers.each do |teacher| %>
         <tr>
           <td><%= teacher.name %></td>
           <td><%= teacher.email %></td>
           <td><%= link_to 'show', teacher_path(teacher) %>

         </tr>
       <% end %>
     </tbody>
    </table><br>

    <%= link_to 'choisissez un nouveau moniteur', school_add_contract_path(@school) %>

    <div id="map" style="width: 50%; height: 300px;"></div>

    <% content_for(:js) do %>
      <%= javascript_tag do %>
        $(document).on('ready', function() {
          handler = Gmaps.build('Google');
          handler.buildMap({ internal: { id: 'map' } }, function(){
            markers = handler.addMarkers(<%= raw @markers.to_json %>);
            handler.bounds.extendWith(markers);
            handler.fitMapToBounds();
          });
        })
      <% end %>
    <% end %>
</div>
</div>

>>>>>>> 43986caad17bd881993a0aadec5e3badfb3bfff2
